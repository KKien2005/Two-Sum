public class Solution {
    public bool IsPalindrome(int x) {
        if(x<0)
            return false;

        string s = x.ToString();
        string reversed = new string(s.Reverse().ToArray());

        return s == reversed;
    }
}
